{
  "name": "solana-rpc-cache-worker",
  "version": "1.0.0",
  "private": false,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.7.5",
    "typescript": "^5.5.2",
    "vitest": "~3.0.7",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.0"
  }
}
